language: java
jdk:
  - openjdk11
#script:
#  - mvn clean install
cache:
  directories:
    - $HOME/.m2
addons:
  sonarcloud:
    organization: "maks45" # the key of the org you chose at step #3
    token:
      secure: "maks45" # encrypted value of your token
script:
  # other script steps might be done before running the actual analysis
  - sonar-scanner